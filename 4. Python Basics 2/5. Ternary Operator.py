# Ternary Operator

# my_var = a if condition else b

age = 5
has_licence = "he can drive" if age >= 18 else "he cannot drive"
# print(has_licence)

is_friend = True
can_message = "Message allowed" if is_friend else "Message not allowed"
print(can_message)
